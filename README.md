# Research Methodology Final Project

Note: kalau mau install dependency (library) baru, jangan lupa tambahin ke dalam requirements.txt ya

Lalu jalankan ini di terminal untuk install semua library setelah clone repo ini
```
pip install -r requirements.txt
```
